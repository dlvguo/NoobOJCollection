#include <iostream>
#include "../../Headers/BinaryTree.h"
#include <stack>
using namespace std;

//SearchX 采用后序遍历

bool isFind=false;
void FindMul(BiTree bt, int key)
{
    if (!bt)
        return;
    
}