#include <iostream>
#include <bits/stdc++.h>
#include <cstring>
#include <vector>
using namespace std;


void dfs(vector<string> ans,)


vector<string> generateParenthesis(int n)
{
    vector<string> ans;
    if(!n)
        return;
    
}
