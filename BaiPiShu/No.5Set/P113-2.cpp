#include<iostream>
using namespace std;
template<class Type>class Set{
    //对象 零个或者多个成员的聚集。其中所有成员的类型是一致的
    int Contains(const Type x);//判断元素是否存在
    int operate==(Set<Type> &right);

}

int main(){
    return 0;
}